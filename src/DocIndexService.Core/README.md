# DocIndexService.Core

Domain layer with entities, enums, constants, interfaces, and shared options.

## Folder Notes

- `Entities`: domain entities used across modules.
- `Enums`: shared enum values.
- `Constants`: role and system constants.
- `Interfaces`: low-level shared abstractions.
- `Options`: strongly typed configuration contracts.
