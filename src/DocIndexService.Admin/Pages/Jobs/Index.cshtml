@page
@model DocIndexService.Admin.Pages.Jobs.IndexModel
@{
    ViewData["Title"] = "Jobs";
}

<h1 class="h4 mb-3">Jobs</h1>
<p class="text-muted">Ingestion queue and execution history shell.</p>

<div class="card">
    <div class="card-body">
        <button class="btn btn-outline-secondary btn-sm" disabled>Retry Failed Job</button>
    </div>
</div>
