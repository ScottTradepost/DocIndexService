# DocIndexService.Api

REST API host exposing versioned endpoints under `/api/v1`.

## Folder Notes

- `Controllers`: thin endpoint handlers.
- `Configuration`: API host-only configuration helpers.
- `Extensions`: API host extension points.
